import React from 'react';
import {render, screen} from '@tests/utils';
import {DatePicker} from './DatePicker';

describe('DatePicker tests', () => {
  it.todo('Did not have enough time to implement it correctly. Sorry!');
  // it('displays provided date', () => {
  //   const date = new Date();

  //   render(<DatePicker date={date} />);

  //   screen.getByDisplayValue(date.toLocaleDateString());
  // });
});
